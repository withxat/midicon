/** Same pinned asset as signal web app (`SoundFontStore.ts`). */
export const signalSoundFontUrl
	= 'https://cdn.jsdelivr.net/gh/ryohey/signal@4569a31/public/A320U.sf2'

export const signalSoundFontName = 'A320U.sf2 (Signal Factory Sound)'

/** Cached copy for local dev / offline; fetch via `pnpm soundfont:fetch`. */
export const localSoundFontPath = '/soundfonts/A320U.sf2'
